/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.ibb.co', 'avatars.githubusercontent.com'], // Add your custom domain here
        // loader: 'imgbb', // Custom loader for ImgBB
    },
};

export default nextConfig;
